{
  "name": "vite-plugin-dev-qrcode-test",
  "private": true,
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite build && vite preview --host"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-dev-qrcode": "workspace:*"
  }
}
